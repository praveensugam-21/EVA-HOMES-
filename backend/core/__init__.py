# ============================================================
# core/__init__.py — Makes 'core' a Python package
# ============================================================
# An __init__.py file (even if empty) tells Python that this
# folder is a "package" — meaning you can import from it.
# Without this file, "from core.config import settings" would fail.
# ============================================================
