# API Base URL for backend (Render / Production)
VITE_API_BASE_URL=https://eva-homes-backend.onrender.com

# Google OAuth Client ID (Google Cloud Console > Credentials > OAuth Client ID > Web application)
# Leave blank to hide the "Sign in with Google" button entirely.
VITE_GOOGLE_CLIENT_ID=
